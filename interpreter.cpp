#include "thread.h"
#include <queue>

using namespace std;

void interpret(char** program) {

    // setup a queue of threads (empty for now)
    queue<Thread> runnable_threads;

    // setup first thread
      // create empty stack for thread
      // set current direction = right
      // find starting point in the program
    
    // interpret program according to instructions

}
